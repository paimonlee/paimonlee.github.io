---
title: '【CSS入门】继承'
date: 2024-12-17 14:30:00
categories:
    - CSS
---


# 继承

## 继承概念
子元素继承父元素的一些CSS属性

```html 
<body>
    <p>由于主题颜色被设置为蓝色，因此该颜色会被子元素继承。</p>
    <p>我们可以通过选择器定位元素来改变颜色，比如这个<span>内容跨越</span>元素。</p>
</body>

<style>
body {
    color: blue;
}

span {
    color: black;
}
</style>
```

不过某些属性是不会继承的，比如width

## 继承控制

CSS提供了五个特殊的属性值来控制继承

1. inherit：继承开关
2. initial：将选定的元素的属性值这是为该属性的初始值
3. revert：重置为浏览器的默认样式，不是属性的默认值，类似于unset
4. revert-layer：设置为父元素的值
5. unset：将属性设置为自然值，如果属性继承那么就是inherit，否则就是initial
6. all：可以同时设置所有继承值
